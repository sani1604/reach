import { useApi } from './api.mjs';

/**
 * Returns information about the editor where the extension is being rendered.
 * @publicDocs
 */
function useExtensionEditor() {
  return useApi().extension.editor;
}

export { useExtensionEditor };
